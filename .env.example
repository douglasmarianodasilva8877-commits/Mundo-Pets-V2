# Copy to .env.local and fill

# Database (Postgres / Neon)
DATABASE_URL="postgresql://user:password@localhost:5432/mundopets"

# NextAuth
NEXTAUTH_SECRET="generate_a_secure_value"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (dev placeholders)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Optional: OpenAI
OPENAI_API_KEY="your-openai-key"

# Supabase (public)
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="pk.eyJ...REPLACE_ME_PUBLIC_ANON_KEY"
# Service role (server only) - do NOT commit real value
# SUPABASE_SERVICE_ROLE="REPLACE_WITH_SERVICE_ROLE"
